#import "@local/note_template:0.1.0": *
#show: doc => note_template([Gradient Descent], doc)

#title()

The *gradient descent* algorithm is a numerical approach to solve optimization
problems like _least squares_.
